# console

Next.js 15 app-router operator UI for the AI Coding Platform.

**Status:** placeholder. Contains only a trivial root layout and page so `pnpm dev` runs; no real UI yet.

Filled in starting Phase 2 — the console is out of scope for the Phase 0 task queue in [`docs/tasks/phase-0/`](../../docs/tasks/phase-0/).
