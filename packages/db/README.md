# platform-db

SQLAlchemy models and Alembic migrations for the platform's structured state.

**Status:** placeholder. This module currently contains only a stub so the
workspace imports cleanly and `make test` passes; there is no real logic yet.

Filled in by [`01-postgres-schema.md`](../../docs/tasks/phase-0/01-postgres-schema.md).
