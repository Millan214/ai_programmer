[project]
name = "platform-shared"
version = "0.0.1"
description = "Cross-cutting utilities shared across services and agents."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/platform_shared"]
